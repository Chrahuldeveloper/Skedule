import React from 'react'

export default function Bar({width, height}) {
  return (
    <div className={`bg-zinc-800 px-2 py-1.5 w-${width} h-${height} border-[1px] border-zinc-800 rounded-xl`}>
      
    </div>
  )
}
